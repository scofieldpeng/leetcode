package bestTimeToBuyAndSellStockII

func maxProfit(prices []int) int {
	var (
		currentProfit = 0
		maxProfit     = 0
		length        = len(prices)
	)

	for i := 1; i < length; i++ {
	}

	return maxProfit
}
